<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pixcel.app.commonfile.mapper.CommonFileMapper">

    <select id="selectMaxFileVersion" resultType="int">
        select nvl(max(file_version), 0)
        from attach_file
        where project_id = #{projectId}
          and connect_address = #{connectAddress}
          and original_name = #{originalName}
    </select>

    <insert id="insertFile" parameterType="com.pixcel.app.commonfile.service.CommonFileVO">
        <selectKey keyProperty="fileId" resultType="string" order="BEFORE">
            select
                'ATTACH_FILE_'
                || to_char(sysdate, 'YYMM')
                || '_'
                || lpad(
                    nvl(max(to_number(substr(file_id, -4))), 0) + 1,
                    4,
                    '0'
                )
            from attach_file
            where file_id like 'ATTACH_FILE_' || to_char(sysdate, 'YYMM') || '_%'
        </selectKey>

        insert into attach_file (
            file_id,
            project_id,
            version_id,
            file_code,
            original_name,
            stored_name,
            file_path,
            file_size,
            upload_user_id,
            file_version,
            connect_address
        ) values (
            #{fileId},
            #{projectId},
            #{versionId},
            #{fileCode},
            #{originalName},
            #{storedName},
            #{filePath},
            #{fileSize},
            #{uploadUserId},
            #{fileVersion},
            #{connectAddress}
        )
    </insert>

</mapper>
